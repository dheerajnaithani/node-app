version: 0.0
os: linux
files:
  - source: /
    destination: /opt/xeniuser/
    overwrite: true
hooks:
  BeforeInstall:
    - location: scripts/before-install.sh
      timeout: 300
  AfterInstall:
    - location: scripts/after-install.sh
      timeout: 300

